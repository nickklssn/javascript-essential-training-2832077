/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */

const but = document.querySelectorAll("button[class = lid-toggle]");

const list = document.querySelector("[class = feature_backpack__strap][data-side = right]")

const div = document.querySelector("header div:last-child").innerHTML


console.log(list);
console.log(but);
console.log(div);


